{
    "name": "ada-2-js",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc --project tsconfig.json",
        "start:client": "ts-node -r dotenv/config src/client.ts",
        "start:worker": "ts-node -r dotenv/config src/worker.ts"
    },
    "dependencies": {
        "amqplib": "^0.8.0",
        "dotenv": "^10.0.0"
    },
    "devDependencies": {
        "@types/amqplib": "^0.8.0",
        "ts-node": "^10.0.0",
        "typescript": "^4.3.2"
    }
}
